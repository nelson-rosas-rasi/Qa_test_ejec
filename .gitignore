node_modules/
dist/
test-results/
playwright-report/
.last-test-results.json

# Los .env reales nunca se versionan…
.env
.env.*
# …pero los del repo de juguete no tienen secretos y son parte de las pruebas.
!electron-app/test-fixtures/sample-repo/.env.*

app-run.log

# Datos de una base Postgres local (no versionar)
.postgres-data/

# Prefijo de Wine para probar el instalador de Windows (~580 MB, se regenera solo)
electron-app/.wine-runqa/

# Worktrees y ajustes locales de Claude Code
.claude/worktrees/
.claude/settings.local.json
